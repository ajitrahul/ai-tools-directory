/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here if needed.
  // For now, an empty object is perfectly fine.
};

export default nextConfig;